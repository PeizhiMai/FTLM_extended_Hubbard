---
description: FTLM extended Hubbard — memory-first C++/Python conventions
globs: "**/*.{hpp,h,cpp,cc,py,cmake,txt,md}"
alwaysApply: true
---

# Project intent

- Target **4×4** (16 sites) FTLM on ~**128 GB** RAM: **no dense Hamiltonians**, **no full spectrum storage**, **fixed small Lanczos workspace**.
- Geometry: **square/rectangular** extended Hubbard (\(t, t', U, V\)); optional twists \(\phi_x,\phi_y\) like the prior [PeizhiMai/FTLM](https://github.com/PeizhiMai/FTLM) design.
- Prefer **sparse `H|v>`** or on-the-fly connectivity; **symmetry blocks** (particle sectors + momentum when available) to reduce dimension.
- Python: **pybind11** thin layer; heavy numerics stay in C++.
- When porting from the old repo, **audit allocations**: peak memory should scale like **O(dim of largest block)** for a few vectors, not **O(samples × lanczos_steps × dim)** unless explicitly cached to disk with a budget.

## Current codebase anchors

- `FockBasis` + `apply_extended_hubbard` + `lanczos_extrema` are implemented.
- **FTLM / n(μ)**: `ftlm_log_partition_*`, Ritz helpers (`ftlm_tridiagonal_ritz_from_lanczos_coeffs`, `ftlm_log_trace_exp_beta_ritz`), and `ftlm_grandcanonical_density_mu_grid` (full observable pipeline still evolving).

# Benchmark regression discipline

- After every substantial code change in FTLM/momentum paths:
  1) rebuild (`cmake --build build -j8`)
  2) run `benchmarks/run_3x2_three_way_monitoring.sh`
  3) verify numerics match baseline:
     - max|nonmom-ED|
     - max|k-ED|
     - max|nonmom-k|
  4) report peak RSS and wall_time_s
- Make exactly one substantial memory change per step.
- If numerics differ, stop and fix before continuing.
